# STEP 06 애플리케이션 설계 — 계획

> 산출물: `aidlc-docs/inception/application-design/components.md`
> 근거: `requirements.md` · `stories.md` (S-1~S-16) · `execution-plan.md` (단위 ①~④ · CONSTRUCTION 03·04 건너뜀)

## 이 단계를 왜 돌리나

조건 단계지만 실행한다. `execution-plan.md` 에서 돌리기로 판정했다.
CONSTRUCTION 03 비기능 설계를 건너뛰기로 했으므로, 아래 두 개가 갈 곳이 여기밖에 없다.

- **NFR-5.1** 외부 지도 · 길찾기 · 장소 검색 접근을 한 경계 뒤에 모아 갈아끼울 수 있게 한다
- **NFR-5.3** 동선 최적화 · 중간지점 계산 · 알람 예약을 독립된 단위로 나누고 화면 코드에 계산을 섞지 않는다

작업 단위를 네 덩어리로 나누기로 했으므로(Q1-C), 그 경계를 그리는 것도 이 단계의 일이다.

## 무엇을 할지

- [x] 구성 요소를 나누고 각각이 **무엇을 책임지는지** 한 줄로 적는다 — C-1~C-8, 화면 V-1~V-9
- [x] 구성 요소 사이에 무엇이 오가는지 (경계면) 적는다 — 함수 이름이 아니라 주고받는 것의 의미로 — 흐름 두 개(동선 받기 · 약속 장소 정하기)로 그림
- [x] 외부 지도 · 길찾기 · 장소 검색 접근 경계를 그린다 (NFR-5.1) — C-7 지도 창구 · C-8 장소 창구 두 개 (Q3-C)
- [x] 계산 · 화면 · 저장을 어떻게 가를지 정한다 (NFR-5.3) — 두 층 (Q2-A). 계산은 C-1~C-4 에만, 화면은 그리기만
- [x] `execution-plan.md` 의 단위 ①~④ 가 어느 구성 요소를 가져가는지 표로 맞춘다
- [x] 설정 값(캐시 유지 시간 · 검색 반경 · 여유 시간 기본값)이 어디에 사는지 정한다 (NFR-5.2) — C-6 설정에 모음
- [x] 무엇을 어떤 방식으로 검증할지 정한다 (NFR-3.1~3.3) — 계산 요소만 단위 테스트, 화면은 손으로 (Q5-A). 명령은 CONSTRUCTION 02 이후
- [x] S-1~S-16 이 모두 어느 구성 요소에 담기는지 대응표를 만든다 — 16개 전부 대응

## 무엇을 하지 않나

- **언어 · 프레임워크 · 라이브러리 · 외부 서비스 이름을 고르지 않는다** — CONSTRUCTION STEP 02 `tech-stack-decisions.md` 한 곳에서만 고른다
- 빌드 · 테스트 **명령**을 적지 않는다 (스택이 정해지지 않았다). 무엇을 검증할지만 정한다
- 함수 이름 · 파일 이름 · 데이터 타입을 정하지 않는다 — CONSTRUCTION 01 기능 설계
- 작업 단위를 확정하지 않는다 — STEP 07
- 코드를 만들지 않는다

---

## 확인 질문

> 한 질문에 하나만 고른다. 답을 `[Answer]:` 뒤에 적고 저장하면 이어서 `components.md` 를 만든다.
> 어느 답을 골라도 언어 · 프레임워크 선택과는 무관하다.

## Q1. 화면을 어떻게 나누나

- A) 한 화면에 다 놓는다 — 일정 목록과 지도 · 동선 결과를 좌우로 붙여 한눈에 본다
- B) 화면을 나눈다 — 일정을 넣는 화면과 동선 · 지도를 보는 화면이 따로 있고 오간다
- C) 하루 보기 하나를 기본으로 두고, 일정 상세 · 약속 중간지점 · 장소 후보는 필요할 때 열리는 패널로 띄운다
- D) Other (please describe after [Answer]: tag below)

[Answer]:C

## Q2. 계산과 화면을 몇 층으로 가르나 (NFR-5.3)

- A) 두 층 — 화면 / 그 밖의 모든 것(계산 · 저장 · 외부 접근)
- B) 세 층 — 화면 / 계산(업무 규칙) / 바깥과 닿는 것(저장 · 외부 서비스)
- C) 네 층 — 화면 / 화면이 쥔 상태 / 계산(업무 규칙) / 바깥과 닿는 것
- D) Other (please describe after [Answer]: tag below)

[Answer]:A

## Q3. 외부 지도 · 길찾기 · 장소 검색 접근을 몇 개 경계로 두나 (NFR-5.1)

- A) 하나 — 지도 · 길찾기 · 장소 검색을 한 경계 뒤에 다 넣는다. 갈아끼울 때 한 군데만 고친다
- B) 셋 — 주소↔좌표 바꾸기 / 길찾기(이동 시간) / 장소 검색 을 각각 나눈다. 서비스를 섞어 쓸 수 있다
- C) 둘 — 지도 관련(주소↔좌표 · 길찾기) / 장소 검색
- D) Other (please describe after [Answer]: tag below)

[Answer]:C

## Q4. 일정 데이터를 어떤 모양으로 두나 (S-16 반복 일정이 걸린다)

- A) 날짜별로 묶는다 — 날짜 하나에 그날 일정 목록이 딸린다
- B) 일정을 평평하게 늘어놓고 날짜로 걸러 본다
- C) 되풀이되는 규칙과 그날 하루치 일정을 따로 두고, 화면에서 합쳐 보여준다
- D) Other (please describe after [Answer]: tag below)

[Answer]:C

## Q5. 무엇을 어디까지 검증하나 (NFR-3.1~3.3)

- A) 계산만 — 동선 최적화 · 중간지점 · 출발시각 계산의 단위 테스트. 화면은 손으로 확인한다
- B) 계산 + 화면 주요 흐름 — 일정 넣고 동선 받고 알림이 잡히는 흐름까지 자동으로 확인한다
- C) 거기에 외부 서비스 경계까지 — 외부가 이런 모양으로 답한다는 약속이 깨지면 테스트가 잡아낸다
- D) Other (please describe after [Answer]: tag below)

[Answer]:A
